# 가비아 웹호스팅 배포

이 사이트는 서버나 DB가 없는 **정적 사이트**다. 빌드 결과물(`dist/`)을
가비아 웹호스팅의 웹 루트에 그대로 올리면 동작한다.
GitHub 저장소는 소스 백업 용도로만 유지한다.

## 1. 이미지 준비 (최초 1회, 중요)

로고·제품 이미지가 아직 외부 임시 URL(AI Studio)에 의존하고 있다.
그대로 배포하면 URL 만료 시 이미지가 통째로 깨진다.

```bash
npm run assets:fetch
```

`src/assets/` 에 `logo.*`, `heroSplash.*`, `productBundle.*`, `productThumb.*`
4개 파일이 생기면 성공이다. 이 파일들은 커밋해 둔다.

URL이 이미 만료돼 스크립트가 실패하면, 원본 이미지를 직접 구해
같은 이름으로 `src/assets/` 에 저장하면 된다. 파일이 놓이는 즉시
빌드가 외부 URL 대신 로컬 파일을 사용한다.

확인 방법 — 빌드 후 아래 명령이 아무것도 출력하지 않으면 외부 의존이 없는 것이다.

```bash
grep -o 'lh3\.googleusercontent\.com' dist/assets/*.js
```

## 2. 빌드

```bash
npm ci
npm run build
```

`dist/` 안에 `index.html`, `assets/`, `favicon.svg`, `.htaccess` 가 생성된다.
`.htaccess` 는 캐시·압축 설정이며 숨김 파일이므로 업로드 시 빠뜨리지 않도록 주의한다.

## 3. 업로드

### 방법 A — 스크립트 (권장)

```bash
cp .env.deploy.example .env.deploy   # FTP 접속 정보 입력
npm run deploy:gabia
```

`.env.deploy` 는 커밋되지 않는다. 접속 정보는 가비아 My가비아 →
웹호스팅 → 관리 화면에서 확인한다.

### 방법 B — FTP 클라이언트 수동 업로드

FileZilla 등으로 접속해 `dist/` **안의 내용물**을 웹 루트에 올린다.
(`dist` 폴더 자체가 아니라 그 안의 파일들이다.)

- 웹 루트: 보통 `/html`, 상품에 따라 `/public_html`
- 숨김 파일 표시를 켜서 `.htaccess` 도 함께 올린다

## 4. 확인

브라우저에서 http://massi.kr/ 접속 후 강력 새로고침(Ctrl/Cmd+Shift+R).

- 페이지가 뜨는지
- 로고·제품 이미지가 보이는지
- 탭 아이콘(favicon)이 뜨는지
- 개발자도구 Network 탭에 404 나 차단된 요청이 없는지

## 5. HTTPS 적용

외부 리소스(웹폰트·이미지)가 모두 https를 쓰므로 혼합 콘텐츠 문제는 없다.
서버에 인증서만 적용하면 바로 전환된다. **반드시 이 순서를 지킨다.**

1. **인증서 발급·적용** — My가비아 → 웹호스팅 → 관리 → SSL 인증서 신청/설정.
   가비아가 제공하는 무료 SSL(상품에 따라 제공) 또는 유료 인증서를 신청한다.
   적용까지 보통 수십 분 걸린다.
2. **적용 확인** — 브라우저에서 `https://massi.kr` 접속. 자물쇠가 뜨고
   인증서 경고가 없어야 한다. 여기서 실패하면 3번으로 넘어가지 않는다.
3. **http -> https 강제 전환** — `public/.htaccess` 상단의 주석 처리된
   리다이렉트 블록에서 각 줄의 `#` 을 지운 뒤 다시 빌드·업로드한다.
   (서버의 `.htaccess` 를 직접 편집해도 되지만, 다음 배포 때 덮어써지므로
   저장소 쪽도 함께 고쳐 두는 편이 안전하다.)
4. **검증** — `http://massi.kr` 로 접속했을 때 `https://` 로 301 전환되는지 확인한다.

인증서가 없는 상태에서 3번을 먼저 켜면 사이트 접속이 인증서 오류로 막힌다.

> HSTS(`Strict-Transport-Security`)는 https가 안정적으로 동작하는 것을
> 충분히 확인한 뒤에 추가한다. 한 번 적용하면 브라우저가 설정 기간 동안
> http 접속을 거부하므로 되돌리기 어렵다.

## 참고

- 빌드는 상대 경로(`base: './'`)를 쓰므로 하위 디렉터리에 올려도 동작한다.
- 웹폰트는 Google Fonts CDN에서 받는다. 완전한 오프라인 자립이 필요하면
  폰트 파일을 내려받아 `src/` 에 두고 `index.html` 의 link 태그를 교체한다.
- 해시 앵커만 쓰는 단일 페이지라 SPA 라우팅용 rewrite 설정은 필요 없다.
